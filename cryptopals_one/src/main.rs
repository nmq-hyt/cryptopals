// the rust use command; base64::prelude
// base 64 is the library,
// A prelude is a collection of names that are automatically brought into scope of every module in a crate. (from the rust reference)
mod tests;
pub mod utils;
fn main() {
   println!("Hello, world!");
}
